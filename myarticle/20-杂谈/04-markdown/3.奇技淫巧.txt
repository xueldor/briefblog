在  “2.语法概述.md”  中，我为了能够在```xxxx```中间嵌套一个````bbb````,如下：

```
```
wenben
```
```
默认第一行与第二行匹配，第四行与第五行匹配。我希望第一行与第四行匹配，中间的```当作普通的文本而不是markdown的标志。
markdown支持反斜杠转义，不过```之间的是代码块，所以markdown不会去转义。  
typora帮我在第二行的开头添加了一个ZWSP符号解决了这个问题。关于ZWSP，参考 “零宽字符.md” 。
